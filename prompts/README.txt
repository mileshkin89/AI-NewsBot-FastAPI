Prompts for news post generation.
You can edit these files without modifying the code.

- system.txt — system instruction for the AI (role, formatting rules, etc.)
- user.txt — instruction for each post (e.g. "Rewrite in 2 sentences")

After editing the files, restart the web container:
  docker compose restart web
